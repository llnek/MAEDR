Sample program demonstrating the implementation of a simple server 
which can accept a TCP connection.

1) The client portion of the program will send "hello world" to the server.

2) The server gets the message and prints it out to the console.

